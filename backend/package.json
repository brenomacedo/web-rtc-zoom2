{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.4.2"
  },
  "dependencies": {
    "socket.io": "^4.2.0"
  },
  "scripts": {
    "dev": "ts-node-dev --ignore-watch node_modules --respawn --transpile-only src/server.ts"
  }
}
